import React, { useState, useEffect } from 'react';
import { motion } from 'framer-motion';
import { Brain, Users, Droplets, Zap, X, ExternalLink } from 'lucide-react';
import { faker } from '@faker-js/faker';

const DSS = () => {
  

  return (
    <div>This is DSS Page</div>
  );
};

export default DSS;
